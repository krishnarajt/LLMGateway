apiVersion: v1
kind: Service
metadata:
  name: llmgateway
spec:
  type: NodePort
  selector:
    app: llmgateway
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 32007